const User = require("../Models/user");
const PDF = require("../Models/pdf");
const shortid = require("shortid");

// ADD PDF

const addPDF = async (req, res) => {
  const { name, fileUrl, sharedUsers } = req.body;
};
